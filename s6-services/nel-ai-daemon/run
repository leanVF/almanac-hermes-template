#!/bin/sh
# Servicio s6 para el daemon de Nel AI (Chatwoot polling).
# Exporta HERMES_HOME explícitamente para que el daemon use el path correcto
# y los subprocess de Hermes que invoque también lo hereden.

export HERMES_HOME=/opt/data

exec python3 /opt/nel-ai/daemon/nel-chatwoot-daemon.py 2>&1
